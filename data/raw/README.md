# Raw Data

The raw CSV is intentionally not committed to GitHub.

Run:

```bash
python -m src.download_data
```

The script retrieves UCI Machine Learning Repository dataset 851 and writes:

```text
Steel_industry_data.csv
```

The source and attribution details are documented in `DATA_SOURCES.md`.
